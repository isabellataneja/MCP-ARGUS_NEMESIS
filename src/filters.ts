/** MDS rows: production constraint for Bangladesh AX Dhaka hub. */
export const BD_MDS_FILTER = { service_provider: 'AX-BD-Dhaka' } as const;

/** Clinician rows: production constraint for Bangladesh sites. */
export const BD_CLINICIAN_SITE_PATTERN = 'AX-BD-%';
